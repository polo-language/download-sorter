apply plugin: 'groovy'

repositories {
  mavenCentral()
}

dependencies {
    compile localGroovy()
    testCompile 'junit:junit:4.12'
}

/*sourceSets {
  main {
    groovy {
      srcDirs = ['src/main/groovy/']
    }
  }

  test {
    groovy {
      srcDirs = ['src/test/groovy']
    }
  }
}*/

//task testSources {
  //sourceSets.getAllSource().each { println "$it" }
//  println sourceSets.main.groovy
//}
